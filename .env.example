# Database configuration - do NOT commit secrets. Copy to .env and fill values.
# Example values:
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=cloud_devops

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cloud_devops

# Service discovery for containers
DB_HOST=db
DB_PORT=5432

# Constructed DATABASE_URL for the api service. You can override this value
# if you prefer to use a different connection string. Keep the literal $VAR
# tokens so the shell will expand them when used locally; some systems require
# you to interpolate values in your own scripts before using.
DATABASE_URL=postgresql+psycopg2://$POSTGRES_USER:$POSTGRES_PASSWORD@$DB_HOST:$DB_PORT/$POSTGRES_DB
